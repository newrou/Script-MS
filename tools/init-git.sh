git config --global user.name "Alex Root"
git config --global user.email newrou@mail.ru
git config --global core.editor mcedit
git config --global merge.tool vimdiff
git config --list

#git init
#git add *.php
#git add *.sh
#git commit -m 'initial project version 0.0'
